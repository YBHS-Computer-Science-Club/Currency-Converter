package currencyconverter.service;

public class ConversionCalculator {

    /**
     * TODO for Beginner:
     * 1. If the amount is less than 0, return -1.0 (error).
     * 2. If the rate is -1.0 (meaning not found), return -1.0 (error).
     * 3. Otherwise, multiply the amount by the rate and return the result.
     */
    public double calculateTotal(double amount, double rate) {
        // Implement logic here
        return 0.0;
    }
}